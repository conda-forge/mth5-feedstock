{% set name = "mth5" %}
{% set version = "0.6.6" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/mth5-{{ version }}.tar.gz
  sha256: cb34e5640f0a2e5d4836cb1b2113ec43fd806b2bac07a6e0ad34e811f0c0c99b

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip

  run:
    - python {{ python_min }}.*
    - setuptools <70
    - h5py
    - matplotlib-base
    - numpy
    - obspy
    - pandas
    - pyyaml
    - scipy
    - xarray
    - mt-metadata >=1.0.0
    - email-validator
    - loguru

test:
  imports:
    - mth5
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip
    - setuptools <70

about:
  home: https://github.com/kujaku11/mth5
  summary: Archivable and exchangeable format for magnetotelluric data
  license: MIT
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - kujaku11
    - kkappler
    - lheagy
